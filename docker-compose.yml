version: "3"

services:
  klaatu:
    build:
      dockerfile: Dockerfile.desktop
      args: 
        FIREFOX_VERSION: ${FIREFOX_VERSION:--nightly}
    volumes:
      - ./test_experiments:/code/test_experiments
      - ./tests:/code/tests
    links:
      - ping-server
  ping-server:
    build: ping_server/
    ports:
      - "5000:5000"
